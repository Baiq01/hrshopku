# Frontend (React)

This is a minimal React app (Vite/CRA-style) that consumes the Laravel API.

Quick start (example using npm):

1. Install dependencies:

   npm install

2. Start dev server:

   npm run start

Notes:
- Update API base URL in `src/config.js` if backend runs on different host/port.
